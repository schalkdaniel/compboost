#' @param stop_args (`list(2)`)\cr
#'   List containing two elements `patience` and `eps_for_break` which can be set
#'   to use early stopping on the left out data from setting `oob_fraction`.
#'   If `! is.null(stop_args)`, early stopping is triggered.
