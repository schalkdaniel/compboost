#' @field idx_train (`integer()`)\cr
#' An index vector used to split `data` into `data = data[idx_train, ]` and `data_oob = data[idx_oob, ]`.
#' Note: `oob_fraction` is ignored if this argument is set.
